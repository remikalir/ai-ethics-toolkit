image: node:lts
create-pages:
pages:
publish: dist
rules:
- if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
before_script:
- npm install
script:
- sed -i "s|/ai-ethics-toolkit/|/|g" vite.config.js
- npm run build
